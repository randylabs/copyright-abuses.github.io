apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
data:
  {{- range $key, $value := .Values.envvars }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
